# rules/

Governance rules, ported from clo-author's 13-rule backbone and extended:

- **Ported:** `permissions.md` (agent capability matrix — single source of truth), `quality.md` (weighted scoring, gate thresholds), `agents.md` (pairing / separation-of-powers / 3-strikes), `workflow.md`, `lifecycle.md`, `content-invariants.md`, `content-standards.md`, `revision.md`, `logging.md`, `working-paper-format.md`, `wiki-integration.md`, `meta-governance.md`.
- **New:** `folder-map.md` (numbered project scheme → the single path reference all agents/hooks read), `output-discipline.md` (update-over-create; new file only for a genuinely new question/literature).
