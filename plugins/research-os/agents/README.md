# agents/

Worker↔critic agent pairs, ported from clo-author's 22-agent roster (kept in full) with ARS capabilities (integrity gate, anti-sycophancy/frame-lock, PRISMA, citation triangulation) merged into the relevant agents. Critics stay read-only (`tools: Read, Grep, Glob`); creators get Write/Edit/Bash. All paths rewired to the numbered project folder scheme.

Roster: `orchestrator`, `librarian`(+critic), `explorer`(+critic), `strategist`(+critic), `theorist`(+critic), `coder`(+critic), `data-engineer`, `writer`(+critic), `writer-dz`, `editor`, `domain-referee`, `methods-referee`, `storyteller`(+critic), `verifier`, `guide-writer`.
